import{AdminAction}from"@/components/admin-actions";import{AdminHeading,AdminTable,Status}from"@/components/admin-ui";import{adminCreators}from"@/lib/admin/repository";
export default async function Page(){const creators=await adminCreators();return <><AdminHeading eyebrow="CREATOR OPERATIONS" title="Creators" description="ReplyPass approval and Stripe payout eligibility remain separate checks."/><AdminTable head={["Creator","Application","Stripe","Products","Activity","Controls"]}>{creators.map((c:any)=>{const profile=Array.isArray(c.profiles)?c.profiles[0]:c.profiles;const stripe=Array.isArray(c.creator_stripe_accounts)?c.creator_stripe_accounts[0]:c.creator_stripe_accounts;return <tr key={c.id}><td><strong>{profile?.display_name}</strong><small>@{c.handle} · {c.verified?"verified":"not verified"}</small></td><td><Status tone={c.status==="approved"?"good":c.status==="suspended"?"bad":"warn"}>{c.status}</Status></td><td><Status tone={stripe?.ready?"good":"warn"}>{stripe?.ready?"eligible":"not eligible"}</Status></td><td>{(c.creator_pricing||[]).filter((p:any)=>p.active).map((p:any)=>p.kind.replace("_"," ")).join(", ")||"None"}</td><td>{(c.paid_interactions||[]).filter((i:any)=>["captured","completed"].includes(i.status)).length} completed · {(c.subscriptions||[]).filter((s:any)=>["active","trialing"].includes(s.status)).length} VIP</td><td><div className="admin-controls">{c.status!=="approved"&&<AdminAction endpoint={`/api/admin/creators/${c.id}`} action="approve" label="Approve" confirm={`Approve @${c.handle} for monetization?`}/>} {c.status!=="rejected"&&<AdminAction endpoint={`/api/admin/creators/${c.id}`} action="reject" label="Reject" reason/>} {c.status==="suspended"?<AdminAction endpoint={`/api/admin/creators/${c.id}`} action="unsuspend" label="Unsuspend"/>:<AdminAction endpoint={`/api/admin/creators/${c.id}`} action="suspend" label="Suspend" reason confirm={`Suspend @${c.handle}?`}/>} {c.verified?<AdminAction endpoint={`/api/admin/creators/${c.id}`} action="remove_verification" label="Remove check"/>:<AdminAction endpoint={`/api/admin/creators/${c.id}`} action="restore_verification" label="Verify"/>}</div></td></tr>})}</AdminTable></>}
