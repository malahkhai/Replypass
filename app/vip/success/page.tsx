import Link from "next/link";import {requireRole} from "@/lib/auth/session";import {serviceDatabase} from "@/lib/stripe/server";export const metadata={title:"VIP membership",robots:{index:false,follow:false}};export default async function Page(){const viewer=await requireRole(["fan","creator","admin"],"/vip/success");let active=false;if(!viewer.demo){const {data}=await serviceDatabase().from("subscriptions").select("status").eq("fan_id",viewer.id).in("status",["active","trialing"]).limit(1).maybeSingle();active=!!data;}return <main className="policy-page"><h1>{active?"Welcome to VIP 💖":"We’re confirming your membership."}</h1><p>{active?"Your private posts and VIP updates are unlocked.":"Stripe is confirming the payment. Refresh your subscriptions shortly; access is never granted from this page alone."}</p><div className="editor-actions"><Link className="button" href="/vip">View VIP posts</Link><Link className="button button-secondary" href="/account/subscriptions">Manage membership</Link></div></main>}
